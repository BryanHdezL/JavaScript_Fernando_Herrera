// Hola mundo en JS

// console.log('Hola mundo!')

// Variables en JS

const c = 10 // Forma para declarar constantes
let a = 10 // Forma recomendada de declarar variables
var b = 10 // Forma antigua utilizada para declarar variables

// console.log('a:', a)
// console.log('b: ' + b)
// console.log(`c: ${c}`)
// console.log({ a, b, c })
// console.table({ a, b, c })

/****************************************************************************
 
                        -----------------------
                                NOTAS
                        -----------------------

- Diferencias entre VAR y LET
    - var:  Funciona dentro de toda la función donde se declara, permite
            volver a declararse, funcionando dentro del objeto window.
    - let:  Solo existe dentro del scope donde se define, no permite
            redeclaración y da error si se usa antes de declararse.
- ECMAScript
    ECMA es el estándar que rige las reglas del lenguaje de programación de 
    JavaScript cada año se publica una version nueva del ECMA, con pequeñas
    reglas o funcionalidades añadidas.

- Polyfill
    Es un código que provee el funcionamiento de una nueva característica 
    de JavaScript (ES6), en versiones viejas como ES5.

****************************************************************************/
